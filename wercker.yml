box: java:8
build:
  steps:
    - script:
      name: setup
      code: |
        ./install_node
        npm install -g node-static
        static
    - script:
      name: build
      code: |
        ./gradlew pJT -i --tests LocalStorageSpec
