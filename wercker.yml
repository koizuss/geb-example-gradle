box: java:8
build:
  steps:
    - script:
      name: setup
      code: |
        apt-get install -y git
    - script:
      name: build
      code: |
        ./gradlew pJT -i --tests LocalStorageSpec
